// Define the structure of the client data (optional but helpful)
interface Client {
  id: number;
  name: string;
  phonenumber: string;
  email: string;
  status: string;
  created_at: string;
  updated_at: string;
}

export default Client